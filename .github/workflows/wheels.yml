name: Build and Publish Python Wheels

on:
  push:
    tags:
      - 'v*'  # Attiva su tag versione (es. v0.1.0)
  workflow_dispatch:  # Permette esecuzione manuale
  release:
    types: [published]

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-13, macos-14]  # macos-13=x86_64, macos-14=arm64

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up QEMU for cross-compilation
        if: runner.os == 'Linux'
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Install GCC 13 on Linux
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-13 g++-13
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-13 100
          sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-13 100

      - name: Build wheels
        uses: pypa/cibuildwheel@v2.22
        env:
          CIBW_BUILD: cp310-* cp311-* cp312-*  # Solo Python 3.10+ per semplificare
          CIBW_SKIP: "*-musllinux_* pp*"
          CIBW_ARCHS_LINUX: x86_64
          CIBW_ARCHS_MACOS: x86_64 arm64
          CIBW_ARCHS_WINDOWS: AMD64
          CIBW_BUILD_VERBOSITY: 3  # Aumenta verbosità per vedere gli errori
          # Usa manylinux_2_28 che ha GCC 11+
          CIBW_MANYLINUX_X86_64_IMAGE: manylinux_2_28
          # Configura CMake e compilatore
          CIBW_ENVIRONMENT_LINUX: >
            CMAKE_ARGS="-DPYTHON_BINDING=ON -DCMAKE_POSITION_INDEPENDENT_CODE=ON -DNO_BUILDCACHE=ON"
            CC=/opt/rh/gcc-toolset-12/root/usr/bin/gcc
            CXX=/opt/rh/gcc-toolset-12/root/usr/bin/g++
            PATH=/opt/rh/gcc-toolset-12/root/usr/bin:$PATH
          CIBW_ENVIRONMENT_MACOS: >
            CMAKE_ARGS="-DPYTHON_BINDING=ON -DCMAKE_POSITION_INDEPENDENT_CODE=ON -DNO_BUILDCACHE=ON"
            MACOSX_DEPLOYMENT_TARGET=10.15
          CIBW_ENVIRONMENT_WINDOWS: >
            CMAKE_ARGS="-DPYTHON_BINDING=ON -DCMAKE_POSITION_INDEPENDENT_CODE=ON -DNO_BUILDCACHE=ON"
          # Installa dipendenze
          CIBW_BEFORE_BUILD_LINUX: >
            yum install -y openssl-devel python3-devel gcc-toolset-12-gcc gcc-toolset-12-gcc-c++
          CIBW_BEFORE_BUILD_MACOS: >
            brew install openssl || true
          # Disabilita i test per ora
          CIBW_PROJECT_REQUIRES_PYTHON: ">=3.10"
        with:
          package-dir: ./python

      - uses: actions/upload-artifact@v4
        with:
          name: cibw-wheels-${{ matrix.os }}-${{ strategy.job-index }}
          path: ./wheelhouse/*.whl

  build_sdist:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build sdist
        run: pipx run build --sdist ./python

      - uses: actions/upload-artifact@v4
        with:
          name: cibw-sdist
          path: python/dist/*.tar.gz

  upload_pypi:
    needs: [build_wheels, build_sdist]
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write  # Per trusted publishing
    if: github.event_name == 'release' && github.event.action == 'published'
    
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: cibw-*
          path: dist
          merge-multiple: true

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
